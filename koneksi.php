<?php
	$host = "localhost";
	$user = "root";
	$password = "";
	$nama_database = "db_bagus_npm";
	$db = mysqli_connect($host, $user,$password, $nama_database);
	// Mengecek apakah koneksi berhasil
    //if (!$db) {
        //die("Koneksi ke database gagal: " . mysqli_connect_error());
    //} else {
        //echo "Koneksi ke database berhasil.";
    //}
?>

